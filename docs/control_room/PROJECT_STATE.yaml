# Durable semantic/operational state for the Redline OS project, as
# displayed by Control Room V0.
#
# This file stores meaning, not machine truth: current mission, latest
# checkpoint, validation posture, and whether the project needs founder
# attention. Live Git facts (branch, HEAD, working-tree/tracking
# condition) are deliberately NOT stored here -- Control Room reads those
# fresh from Git on every request. See docs/CONTROL_ROOM_V0_ARCHITECTURE.md.

project_id: redline-os

summary: >
  Redline OS V1 is frozen; Control Room V0 Mission 3 (read-only Project
  Detail screen) is complete and published; the system is ready for the
  next Founder-authorized mission.

current_mission:
  id: control-room-v0-m3
  title: Control Room V0 — Mission 3
  phase: complete

latest_checkpoint:
  label: Control Room V0 Mission 3
  commit: 8f20ac48aedda97fe0a6d228a46f3a9fa3b510d2
  document: docs/control_room/MISSION_3_CLOSURE_2026-08-15.md

validation:
  status: pass
  summary: >
    Control Room V0 Mission 3: added a read-only Project Detail screen
    (client-side hash routing, no new backend route; reuses
    GET /api/projects/{project_id}). Focused Control Room suite: 52
    passed (6 new). Broad regression: 2458 passed, 18 skipped, 4 failed --
    all 4 pre-existing/environment-specific, none touching control_room,
    matching Mission 1's documented baseline plus exactly the 6 new
    tests. Zero mutation routes confirmed. Live browser verification
    performed. Codex independent review not required per Mission 3
    authorization.

attention:
  required: false
  reason: null
