# Durable semantic/operational state for the Redline OS project, as
# displayed by Control Room V0.
#
# This file stores meaning, not machine truth: current mission, latest
# checkpoint, validation posture, and whether the project needs founder
# attention. Live Git facts (branch, HEAD, working-tree/tracking
# condition) are deliberately NOT stored here -- Control Room reads those
# fresh from Git on every request. Mission history is also deliberately
# NOT stored here -- Control Room parses it fresh from closure documents
# under docs/control_room/. See docs/CONTROL_ROOM_V0_ARCHITECTURE.md.

project_id: redline-os

summary: >
  Redline OS V1 is frozen; Control Room V0 Mission 8 (read-only Current
  Working Tree Change Detail) is complete and published; the system is
  ready for the next Founder-authorized mission.

current_mission:
  id: control-room-v0-m8
  title: Control Room V0 -- Mission 8
  phase: complete

latest_checkpoint:
  label: Control Room V0 Mission 8
  commit: 31d82e05bb3a3f7719a58ce6e50ae950f6631d0e
  document: docs/control_room/MISSION_8_CLOSURE_2026-08-16.md

validation:
  status: pass
  summary: >
    Control Room V0 Mission 8: added read-only Current Working Tree
    Change Detail (repository-relative paths currently staged,
    unstaged, untracked, or mid-conflict, derived from one single
    authoritative git status read via GitReader._read_working_tree) to
    the Project Detail screen's live GitStatus block, alongside the
    existing Checkpoint Change Set Detail (Mission 7). Three
    independent-review correction rounds via a static Codex fallback
    (this machine's Codex OS-level sandbox is broken -- an environment
    gap, not a code defect): empty-path and malformed rename/copy score
    hardening, then per-record-type status-letter scoping plus
    --no-optional-locks. Focused Control Room suite: 139 passed. Broad
    regression (implementation environment): 2536 passed, 18 skipped, 4
    failed -- classified as pre-existing/environmental, unrelated to
    control_room. Zero mutation routes confirmed. Independent Codex
    review verdict: PASS -- READY FOR CHECKPOINT DECISION, after three
    correction/verification rounds.

attention:
  required: false
  reason: null
