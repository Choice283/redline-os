# Durable semantic/operational state for the Redline OS project, as
# displayed by Control Room V0.
#
# This file stores meaning, not machine truth: current mission, latest
# checkpoint, validation posture, and whether the project needs founder
# attention. Live Git facts (branch, HEAD, working-tree/tracking
# condition) are deliberately NOT stored here -- Control Room reads those
# fresh from Git on every request. Mission history is also deliberately
# NOT stored here -- Control Room parses it fresh from closure documents
# under docs/control_room/. See docs/CONTROL_ROOM_V0_ARCHITECTURE.md.

project_id: redline-os

summary: >
  Redline OS V1 is frozen; Control Room V0 Mission 6 (read-only Mission
  Scope & Outcome Detail) is complete and published; the system is
  ready for the next Founder-authorized mission.

current_mission:
  id: control-room-v0-m6
  title: Control Room V0 -- Mission 6
  phase: complete

latest_checkpoint:
  label: Control Room V0 Mission 6
  commit: 1eb2a31a383c0cff56acddb616dd95e9b8c7fe3c
  document: docs/control_room/MISSION_6_CLOSURE_2026-08-15.md

validation:
  status: pass
  summary: >
    Control Room V0 Mission 6: added read-only Mission Scope & Outcome
    Detail (Purpose/Delivered Capability/Deferred Work) to the Project
    Detail mission history, alongside the existing Validation & Evidence
    Detail, parsed fresh from durable closure documents and embedded in
    the existing ProjectSnapshot API. Focused Control Room suite: 94
    passed. Broad regression: 2500 passed, 18 skipped, 4 failed in the
    implementation environment (2722 passed, 18 skipped, 28 failed in
    the independent Codex review environment) -- both classified as
    pre-existing/environmental, unrelated to control_room. Zero
    mutation routes confirmed. Independent Codex review verdict: PASS --
    READY FOR CHECKPOINT DECISION, with one non-blocking coverage note.

attention:
  required: false
  reason: null
