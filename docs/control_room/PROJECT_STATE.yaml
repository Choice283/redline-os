# Durable semantic/operational state for the Redline OS project, as
# displayed by Control Room V0.
#
# This file stores meaning, not machine truth: current mission, latest
# checkpoint, validation posture, and whether the project needs founder
# attention. Live Git facts (branch, HEAD, working-tree/tracking
# condition) are deliberately NOT stored here -- Control Room reads those
# fresh from Git on every request. Mission history is also deliberately
# NOT stored here -- Control Room parses it fresh from closure documents
# under docs/control_room/. See docs/CONTROL_ROOM_V0_ARCHITECTURE.md.

project_id: redline-os

summary: >
  Redline OS V1 is frozen; Control Room V0 Mission 7 (read-only
  Checkpoint Change Set Detail) is complete and published; the system is
  ready for the next Founder-authorized mission.

current_mission:
  id: control-room-v0-m7
  title: Control Room V0 -- Mission 7
  phase: complete

latest_checkpoint:
  label: Control Room V0 Mission 7
  commit: 72cf8a7382e55342d16f0fc7c17c651c9f2d3a07
  document: docs/control_room/MISSION_7_CLOSURE_2026-08-15.md

validation:
  status: pass
  summary: >
    Control Room V0 Mission 7: added read-only Checkpoint Change Set
    Detail (repository-relative file paths changed by each historical
    mission's published checkpoint, derived live from Git via
    GitReader.read_commit_changed_files) to the Project Detail mission
    history, alongside the existing Mission Scope & Outcome Detail and
    Validation & Evidence Detail. Two independent-review correction
    rounds: merge commits now degrade explicitly rather than risk an
    incorrect empty change set, and a successful rev-list result is
    validated token-by-token before use. Focused Control Room suite: 114
    passed. Broad regression (implementation environment): 2517 passed,
    18 skipped, 4 failed -- classified as pre-existing/environmental,
    unrelated to control_room. Zero mutation routes confirmed.
    Independent Codex review verdict: PASS -- READY FOR CHECKPOINT
    DECISION, after both correction rounds.

attention:
  required: false
  reason: null
