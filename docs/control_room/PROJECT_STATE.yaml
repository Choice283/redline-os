# Durable semantic/operational state for the Redline OS project, as
# displayed by Control Room V0.
#
# This file stores meaning, not machine truth: current mission, latest
# checkpoint, validation posture, and whether the project needs founder
# attention. Live Git facts (branch, HEAD, working-tree/tracking
# condition) are deliberately NOT stored here -- Control Room reads those
# fresh from Git on every request. Mission history is also deliberately
# NOT stored here -- Control Room parses it fresh from closure documents
# under docs/control_room/. See docs/CONTROL_ROOM_V0_ARCHITECTURE.md.

project_id: redline-os

summary: >
  Redline OS V1 is frozen; Control Room V0 Mission 5 (read-only
  Validation & Evidence Detail) is complete and published; the system is
  ready for the next Founder-authorized mission.

current_mission:
  id: control-room-v0-m5
  title: Control Room V0 -- Mission 5
  phase: complete

latest_checkpoint:
  label: Control Room V0 Mission 5
  commit: 8d6e97b2417efc95262b9ec341d9c3e439cf5881
  document: docs/control_room/MISSION_5_CLOSURE_2026-08-15.md

validation:
  status: pass
  summary: >
    Control Room V0 Mission 5: added read-only Validation & Evidence
    Detail to the Project Detail mission history, parsed fresh from
    durable closure documents and embedded in the existing ProjectSnapshot
    API. Focused Control Room suite: 83 passed. Fence-specific
    regressions: 3 passed. Broad regression was not rerun for the focused
    correction round; the previous Mission 5 broad gate remains the
    mission-level regression evidence. Zero mutation routes confirmed.
    Independent review correction and focused re-review passed.

attention:
  required: false
  reason: null
