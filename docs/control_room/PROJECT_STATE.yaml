# Durable semantic/operational state for the Redline OS project, as
# displayed by Control Room V0.
#
# This file stores meaning, not machine truth: current mission, latest
# checkpoint, validation posture, and whether the project needs founder
# attention. Live Git facts (branch, HEAD, working-tree/tracking
# condition) are deliberately NOT stored here -- Control Room reads those
# fresh from Git on every request. Mission history is also deliberately
# NOT stored here -- Control Room parses it fresh from closure documents
# under docs/control_room/. See docs/CONTROL_ROOM_V0_ARCHITECTURE.md.

project_id: redline-os

summary: >
  Redline OS V1 is frozen; Control Room V0 Mission 9 (read-only
  Closed-State Currency) is complete and published; the system is
  ready for the next Founder-authorized mission.

current_mission:
  id: control-room-v0-m9
  title: Control Room V0 -- Mission 9
  phase: complete

latest_checkpoint:
  label: Control Room V0 Mission 9
  commit: d321209b424c0b8b3b042a2b7a90508754f963fb
  document: docs/control_room/MISSION_9_CLOSURE_2026-08-16.md

validation:
  status: pass
  summary: >
    Control Room V0 Mission 9: added read-only Closed-State Currency
    (whether the repository has moved beyond the commit that introduced
    the recorded closure document, derived via two-layer closure-path
    validation plus GitReader.read_path_introduction_commit() and
    GitReader.read_closed_state_currency() -- four locked states,
    CURRENT/AHEAD/NOT_ANCESTOR/UNAVAILABLE, observation only, never fed
    into attention) to the Project Detail screen's state/checkpoint
    area. One independent-review correction round: a MEDIUM finding
    (str.isdigit() accepted non-ASCII digit characters that int() then
    rejected with an uncaught ValueError instead of degrading to
    UNAVAILABLE) was corrected with a strict anchored ASCII-decimal
    regex and regression coverage, then independently re-verified,
    including direct reproduction of the previously failing case.
    Mission 9 suite: 55 passed. Focused Control Room suite: 194 passed.
    Real Mission 8 closed-state proof: CURRENT, 0 commits beyond,
    independently reproduced. Zero mutation, zero new routes, zero
    attention-signal integration confirmed. Independent review verdict:
    APPROVE MISSION 9 COMMIT GATE, after one correction/verification
    round.

attention:
  required: false
  reason: null
