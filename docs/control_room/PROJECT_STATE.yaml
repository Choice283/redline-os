# Durable semantic/operational state for the Redline OS project, as
# displayed by Control Room V0.
#
# This file stores meaning, not machine truth: current mission, latest
# checkpoint, validation posture, and whether the project needs founder
# attention. Live Git facts (branch, HEAD, working-tree/tracking
# condition) are deliberately NOT stored here -- Control Room reads those
# fresh from Git on every request. See docs/CONTROL_ROOM_V0_ARCHITECTURE.md.

project_id: redline-os

summary: >
  Redline OS V1 is frozen; Control Room V0 Mission 1 is complete and
  published; the system is ready for the next Founder-authorized mission.

current_mission:
  id: control-room-v0-m1
  title: Control Room V0 — Mission 1
  phase: complete

latest_checkpoint:
  label: Control Room V0 Mission 1
  commit: aa1539f9f3622101e35de87bf37e9fbc4987e9a1
  document: docs/control_room/MISSION_1_CLOSURE_2026-08-15.md

validation:
  status: pass_with_exception
  summary: >
    Control Room V0 Mission 1: independent Codex review verdict PASS --
    READY FOR CHECKPOINT. Focused Control Room validation: 46 passed.
    Broad regression suite completed with documented environment/baseline
    exceptions, unrelated to Control Room. CI for the published checkpoint
    (aa1539f) observed failed: 43 failed, 2670 passed, 7 skipped -- every
    observed failure is pre-existing Windows-hardcoded-path/Python-3.11-
    interpreter/RLC-E9901-evidence-path debt; zero Control Room test
    failures observed.

attention:
  required: false
  reason: null
